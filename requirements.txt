# Ứng dụng Chat LAN - Dependencies
# Chạy: pip install -r requirements.txt

# ==================== Server & Client Common ====================
python-dotenv==1.0.0              # Quản lý biến môi trường từ .env
bcrypt==4.1.2                     # Mã hóa mật khẩu an toàn
Pillow==10.1.0                    # Xử lý ảnh (PIL)
cryptography==41.0.7              # Tạo SSL certificate (generate_cert.py)

# ==================== Client UI ====================
PyQt6==6.6.1                      # Framework UI PyQt6
PyQt6-Qt6==6.6.1                  # Qt6 runtime cho PyQt6
PyQt6-sip==13.6.0                 # Support module cho PyQt6

# ==================== Optional - Development ====================
# pytest==7.4.3                   # Testing framework (nếu cần test)
# black==23.12.0                  # Code formatter (nếu cần format code)
# flake8==6.1.0                   # Linter (nếu cần kiểm tra code style)
